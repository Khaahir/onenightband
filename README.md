# One Night Band — Vite + React + Tailwind + Appwrite

Instructions:

1. Unzip this project
2. cd into folder
3. npm install
4. npx tailwindcss -i ./src/index.css -o ./src/tailwind-output.css (optional)
5. npm run dev

Appwrite:
- Open src/pages/Events.jsx and replace endpoint, project, databaseId, collectionId with your Appwrite values.
- Create a collection with fields date, place, desc (or use the SDK to adapt).
